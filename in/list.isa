module Option
    type Option a = None | Some a;

module List
    type List a = Nil | Cons a (List a);
    
    let rec lenght list =
        match list with
            Nil -> 0,
            Cons _ list -> 1 + lenght list,
    ;
